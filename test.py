import torch

torch_version = torch.__version__.split(".")
# major_torch, minor_torch = int(torch_version[0]), int(torch_version[1])

# print(f"PyTorch Version: {torch.__version__}")
# print(f"Major: {major_torch}, Minor: {minor_torch}")
